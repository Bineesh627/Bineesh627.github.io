import React from 'react';

export const EShopCaseStudy = (
  <>
    <h1>e_shop: Optimized E-Commerce Client Workspace</h1>

    <p>
      A mock commercial store front, focusing on client-side state coherence, swift product searches, dynamic shopping cart calculation, and responsive checkout layout flows.
    </p>

    <h2>The Challenge</h2>
    <p>
      High cart bounce rates often stem from slow checkout processes and clunky state synchronization when items are modified.
    </p>

    <h2>The Solution</h2>
    <p>
      Developed an optimized local cart manager using modular JS handlers, instant item updates, auto-completing search tags, and clean layout rows.
    </p>

    <h2>Key Technical Components</h2>
    <ul>
      <li><strong>Client State Cart:</strong> Handles local item storage and dynamically updates totals.</li>
      <li><strong>Tag Autocompletion:</strong> Text searches immediately index target match words.</li>
      <li><strong>Responsive grid forms:</strong> Smooth form validations on checkout screens.</li>
    </ul>

    <blockquote>
      <p>
        "Moving checkout forms to simple native HTML validations coupled with modular state sync scripts dropped cart load-times by 40%."
      </p>
    </blockquote>
  </>
);
